{
  "$schema": "http://json-schema.org/schema",
  "id": "Schema",
  "title": "Page Options Schema",
  "description": "Generate page module",
  "properties": {
    "name": {
      "type": "string",
      "description": "The name of the module",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "Please name your module"
    }
  },
  "required": [
    "name"
  ]
}
