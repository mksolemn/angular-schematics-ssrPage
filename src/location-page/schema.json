{
  "$schema": "http://json-schema.org/schema",
  "id": "Schema",
  "title": "Location page with SSR",
  "description": "Pre-generate pages describing locations",
  "properties": {
    "path": {
      "type": "string",
      "description": "Path for new module output",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "Where would you like to deploy pages?"
    }
  },
  "required": [
    "path"
  ]
}
